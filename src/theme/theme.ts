import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "light",
    primary: { main: "#1a2023ff" },
    background: {
      default: "#1f2329",
    },
    text: {
      primary: "#ffffff",
      secondary: "#c0c0c0",
    },
  },
});
